{
  "dependencies": { "@prisma/client": "^5.22.0", "dotenv": "^16.4.5", "pg": "^8.13.1" },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "scripts": {
    "dev": "bun run index.js",
    "production": "bun i && cp prisma/schema.production.prisma prisma/schema.prisma && bun prisma generate && bun prisma migrate deploy && bun run index.js"
  }
}